<?xml version="1.0"?>
<project name="PhoneGap NFC Plugin" default="jar" basedir=".">

  <!-- set global properties for this build -->
  <property name="src.dir" location="src"/>
  <property name="build.dir" location="target"/>
  <property file="build.properties"/>
  <property name="target" value="android-10"/>

  <target name="compile" description="build a new version of phonegap-nfc.jar">
    <mkdir dir="${build.dir}"/>
    <delete includeemptydirs="true">
      <fileset dir="${build.dir}" includes="**/*"/>
    </delete>
    <javac srcdir="${src.dir}" destdir="${build.dir}" debug="on" source="1.6" includeantruntime="false">
      <classpath>
        <path location="${sdk.dir}/platforms/${target}/android.jar"/>
        <path location="${phonegap.jar}"/>
      </classpath>
    </javac>
  </target>

  <target name="jar" depends="compile" description="bundle phonegap-nfc">
    <jar destfile="phonegap-nfc.jar" basedir="${build.dir}"/>
  </target>

  <target name="clean" description="clean up">
    <delete dir="${build.dir}"/>
    <delete file="phonegap-nfc.jar"/>
  </target>
</project>
